# 	Programa de prueba 
 	Matrox Compiler
	Edilson Gonzalez y Guillermo Mazzoni #

function integer main[]
	integer test_int <- 10;
	character test_char <- \c\;
	boolean test_bool <- true;
	number test_number <- 10.0;
	string test_string <- "Hola mundo";
	integer a <- 5, b <- 5, c <- 6;
	
	if (test_int = 10 and true)
		print "Hola";
	elseif(true)
		print "Es diferente";
	end

	while(true)
		print 1-2*3/5;
		print (not (1 < 2 or 1 <= 2 or 1 > 2 or 1 >= 2 or 1 <> 2 or 1 = 2));
	end

	for (integer i <- 0; i < 10; i /<- 3)
		get_value test_int;
	end
		
	switch (test_int)
		option 1,
			print "One or three";
		end

		option 
			print "Only two";
		end
	end
	holaMundo[test_string];
	give_back 0;
end

# function string holaMundo[string message]
	give_back message;
end
